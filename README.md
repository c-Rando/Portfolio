# Portfolio

    This document is constructed in an effort to convey information regarding this programmer's qualifications.

These documents were constructed using only semantic HTML elements, an exported CSS stylesheet, and an exported CSS reset document.

This document uses cascading styles to effect multiple elements within its within its elements or attributes.